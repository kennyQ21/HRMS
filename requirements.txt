# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9    # required for UploadFile / Form

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.35
alembic>=1.13.0
pymysql>=1.1.1
pymongo>=4.11.1
psycopg[binary]>=3.2.6
psycopg2-binary>=2.9.10
pyodbc>=5.2.0

# ── Auth / HTTP ───────────────────────────────────────────────────────────────
requests>=2.32.3

# ── Data / File Parsing ───────────────────────────────────────────────────────
pandas>=2.2.3
openpyxl>=3.1.5
python-docx>=1.1.2
pypdf2>=3.0.1
pycryptodome>=3.22.0
# azure-ai-documentintelligence>=1.0.0  # disabled — PaddleOCR is the sole OCR engine
numpy>=1.26.4,<2.0.0
pillow>=10.0.0
pdf2image>=1.17.0
python-pptx>=1.0.2

# ── Archive support ───────────────────────────────────────────────────────────
py7zr>=0.22.0
rarfile>=4.2
patool

# ── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.8.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
sqlparse>=0.5.0
chardet>=5.2.0
py>=1.11.0
tldextract>=5.1.0

# ── PII detection — GLiNER zero-shot semantic NER ─────────────────────────────
gliner>=0.2.0

# torch CPU-only — installed separately in Dockerfile with PyTorch CPU index

# ── OCR engine — PaddleOCR + CPU-only PaddlePaddle backend ───────────────────
# paddlepaddle 3.3.1 has no ARM64 Linux wheel; floor at 3.0.0 so pip picks the
# best available wheel for the build platform (3.2.2 on aarch64, 3.3.1 on x86_64)
paddlepaddle>=3.0.0
paddleocr>=3.0.0
